apiVersion: v1
kind: Service
metadata:
  name: wishlist-service
  labels:
    app.kubernetes.io/name: wishlist-service
    app.kubernetes.io/part-of: tasktreat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wishlist-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002
      protocol: TCP
