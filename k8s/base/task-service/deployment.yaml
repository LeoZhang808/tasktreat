apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-service
  labels:
    app.kubernetes.io/name: task-service
    app.kubernetes.io/part-of: tasktreat
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: task-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: task-service
        app.kubernetes.io/part-of: tasktreat
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: task-service
          # Short name -- the Kustomize overlay rewrites it to the full ECR URL.
          image: tasktreat-dev-task-service:dev-latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4001
          env:
            - name: PORT
              value: "4001"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: tasktreat-config
                  key: NODE_ENV
            # task-service connects to the `task` schema in RDS.
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tasktreat-db-secret
                  key: DATABASE_URL_TASK
          readinessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
