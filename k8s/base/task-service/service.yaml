apiVersion: v1
kind: Service
metadata:
  name: task-service
  labels:
    app.kubernetes.io/name: task-service
    app.kubernetes.io/part-of: tasktreat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: task-service
  ports:
    - name: http
      port: 4001
      targetPort: 4001
      protocol: TCP
