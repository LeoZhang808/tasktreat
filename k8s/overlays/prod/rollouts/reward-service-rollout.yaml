###############################################################################
# Argo Rollouts replacement for the reward-service Deployment in production.
# Same canary shape as the other two backend services.
###############################################################################
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: reward-service
  labels:
    app.kubernetes.io/name: reward-service
    app.kubernetes.io/part-of: tasktreat
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: reward-service
  strategy:
    canary:
      maxSurge: 1
      maxUnavailable: 0
      steps:
        - setWeight: 20
        - pause:
            duration: 60s
        - setWeight: 50
        - pause:
            duration: 60s
        - setWeight: 100
  template:
    metadata:
      labels:
        app.kubernetes.io/name: reward-service
        app.kubernetes.io/part-of: tasktreat
        app.kubernetes.io/component: backend
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: reward-service
          image: tasktreat-dev-reward-service:dev-latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4003
          env:
            - name: PORT
              value: "4003"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: tasktreat-config
                  key: NODE_ENV
            - name: TASK_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: tasktreat-config
                  key: TASK_SERVICE_URL
            - name: WISHLIST_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: tasktreat-config
                  key: WISHLIST_SERVICE_URL
            - name: REWARD_VALUE_PER_TASK
              valueFrom:
                configMapKeyRef:
                  name: tasktreat-config
                  key: REWARD_VALUE_PER_TASK
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tasktreat-db-secret
                  key: DATABASE_URL_REWARD
            - name: APP_VERSION
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app.kubernetes.io/version']
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
